function  JumpToUrl(url){
    window.location.href=url;
}

function goBackToPreviousPage() {
    window.history.back();
}

function showAlert(message) {
    alert(message); // 弹出提示框
}
function addStandard() {
    // 这里可以编写添加新标准的逻辑，比如打开一个模态框，发送AJAX请求等
    alert('请实现添加标准的逻辑');
}